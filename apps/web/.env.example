VITE_API_URL=http://localhost:3000

# Base path for agent workspaces (each developer sets their own local path)
# The agent will use {VITE_WORKSPACE_BASE_PATH}/{project-name} as the working directory
# VITE_WORKSPACE_BASE_PATH=/Users/yourname/path-to-repo/workspaces

# Development Mock Session (set to "true" to skip login during development)
# When enabled, frontend will use a mock user session
# VITE_DEV_MOCK_SESSION=true

# Agent workspace path - the directory where the Claude agent will run
# VITE_WORKSPACE_PATH=/path/to/your/workspace
