# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/berryon

# Better Auth
BETTER_AUTH_SECRET=your-secret-key-change-this-in-production
BETTER_AUTH_URL=http://localhost:3000

# API
PORT=3000
NODE_ENV=development

# Development Mock Session (set to "true" to skip login during development)
# When enabled, API will accept requests without authentication
# DEV_MOCK_SESSION=true

# OAuth Providers (REQUIRED - social login only, no email/password)
# Google: https://console.cloud.google.com/apis/credentials
# Callback URL: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-google-client-secret

# GitHub: https://github.com/settings/developers
# Callback URL: http://localhost:3000/api/auth/callback/github
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# CORS (Production only)
# Set this to your frontend URL in production
# FRONTEND_URL=https://your-app.zeabur.app

# Stripe (OPTIONAL for development - only needed to test payment features)
# Uncomment and fill these values when testing checkout/payment flows
# Get these from https://dashboard.stripe.com/apikeys
# STRIPE_SECRET_KEY=sk_test_your_key_here
# Get this from Stripe CLI: stripe listen --forward-to localhost:3000/api/webhooks/stripe
# Or from Stripe Dashboard: https://dashboard.stripe.com/webhooks
# STRIPE_WEBHOOK_SECRET=whsec_your_secret_here

# Google Vertex AI (REQUIRED for AI image generation)
# Set your Google Cloud project ID
# GOOGLE_CLOUD_PROJECT=your-gcp-project-id
# GOOGLE_CLOUD_LOCATION=us-central1

# Cloudflare R2 (REQUIRED for image storage)
# Get these from Cloudflare Dashboard > R2 > Manage R2 API Tokens
# R2_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
# R2_ACCESS_KEY_ID=your-r2-access-key
# R2_SECRET_ACCESS_KEY=your-r2-secret-key
# R2_BUCKET=berryon-uploads
# R2_PUBLIC_URL=https://pub-<hash>.r2.dev
